After check out code 

Run Below command to install python binaries from (PyCharm or anyother IDE).
Go to your project workspace directory and run below.

pip install fastapi uvicorn

pip install sqlalchemy

pip install psycopg2-binary

pip install python-jose

pip list to see all module install.

To reload and run project

uvicorn main:app --reload
